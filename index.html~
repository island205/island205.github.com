<input type="button" onclick="ily()" value="DO YOU KNOW ?" />
<script>
    function ily() {
				alert("I LOVE YOU.THIS IS MY HEART!");
        var func, line, x, y, _ref, _ref2;
        func = function (x, y) {
            x = x * 1.0 / 30;
            y = -y * 1.0 / 10;
            if ((Math.pow(Math.pow(x, 2) + Math.pow(y, 2) - 1, 3) - Math.pow(x * y, 2) * y) < 0) {
                return true;
            } else {
                return false;
            }
        };
        for (x = _ref = -15; _ref <= 15 ? x <= 15 : x >= 15; _ref <= 15 ? x++ : x--) {
            line = "";
            for (y = _ref2 = -40; _ref2 <= 40 ? y <= 40 : y >= 40; _ref2 <= 40 ? y++ : y--) {
                if (func(y, x)) {
                    line += "<span style='color:red;'>*</span>";
                } else {
                    line += "&nbsp;";
                }
            }
            document.write(line+"<br />");
        }
    }
</script>
